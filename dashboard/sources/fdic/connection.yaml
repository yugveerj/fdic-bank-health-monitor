# BigQuery source. authenticator gcloud-cli = plain Application Default
# Credentials: `gcloud auth application-default login` locally, the WIF
# credential file in CI. No secrets live in this repo either way.
name: fdic
type: bigquery
options:
  project_id: fdic-monitor
  location: US
  authenticator: gcloud-cli
